# Expense Tracker Frontend

React + TypeScript application using Vite and Tailwind CSS.

## Setup

1.  `npm install`
2.  `npm run dev`

## Docker

```bash
docker build -t expense-tracker-frontend .
```